Tru3Bliss – Solana Token Bundler Suite

Репозиторий: 0xMuse9/solana-token-bundler-… (TypeScript, 185★). Мульти-кошелечный бандлер для запуска токенов на Pump.fun (pumpfun) и Bonk.fun, использует Jito для атомарных бандлов
github.com
. Все ключевые инструкции (создание пула, покупки) группируются в Jito-бандл, который обрабатывается в одном слоте. В архитектуре используются Solana Web3.js + Raydium SDK v2 и модуль Jito (см. стек технологий
github.com
). Пример: пул из Pump.fun с 16 кошельками был успешно куплен одним бандлом
github.com
 – таким образом достигается очень высокая сквозная скорость исполнения (вместо последовательных транзакций). Бандлер оптимизирован по задержкам (отправляет большой tip для приоритета) и использует предсказуемое распределение сумм. Подходит для мемекойн-стартапов, где важно быстро «закрыть» раздачу.

Архитектура: Node.js/TypeScript, Jito/Solana-клиент, Асинхронный Engine для формирования и отправки бандлов.

Производительность: Примерно 16 транзакций (кошельков) в одном блоке (Jito Bundle)
github.com
. Это позволяет в разы превзойти обычный TPS.

Применение: Синхронное развёртывание и покупка новых токенов на Pump.fun и Bonk.fun, быстрый снайпинг при листингах.

Отзывы: Проект активно используется трейдерами. Один разработчик отмечает, что сам написал подобный бандлер (tokariapp) и он работает быстрее «дорогих» решений
reddit.com
. Пользователи в целом советуют учиться строить собственные Jito-бандлеры для максимальной эффективности
reddit.com
.

ivorn42 – Markless Pumpfun/Bonkfun Bundler

Репозиторий: ivorn42/markless-pumpfun-bonkfun-bundler (TypeScript, 201★). “Advanced JITO Bundle Solution” для Pump.fun и Bonk.fun, с оптимизацией через Lookup Tables и обходом «пузырей» (bubble maps) при помощи стратегий Axiom/Bloom
github.com
. Позволяет координировать покупки из 24+ кошельков одновременно
github.com
. Ключевая особенность – атомарность: до 4 транзакций объединяются в один Jito-бандл
github.com
. В данном бандлере в одном бандле создаётся пул и до 3 транзакций покупки от 24 кошельков
github.com
, а затем при желании – объединённая продажа. Всё это выполняется «под крышкой» Jito, минимизируя задержки включения.

Архитектура: Node.js/TypeScript, активное использование Address Lookup Table для сокращения размера tx. Jito Relayer инкапсулирует до 4 транзакций в бандл
github.com
.

Производительность: Координатор создаёт и запускает пул вместе с покупками 24 кошельков за один блок
github.com
. Это эквивалентно ~24х последовательных транзакций, но за время одного блока.

Применение: Нативно поддерживает Pump.fun и Bonk.fun, а также решает проблему прозрачности на «пузырьковой карте» – все кошельки выглядят разными. Имеет функции «чистки» карт (отслеживает и скрывает связи кошельков).

Отзывы: Популярен у продвинутых трейдеров Pump.fun. Отмечают, что бандл на 24 кошелька надёжно проходит через Jito, и что этот бот упрощает получение «чистого» холдинга для каждой темы
github.com
.

Rabnail – Pump.fun Bundler (20 wallets)

Репозиторий: Rabnail-SOL/Solana-PumpFun-Bundler (TypeScript, 187★). Простой бандлер для Pump.fun, который запускает пул и покупает токен сразу 20 кошельками в одном блоке
github.com
. Использует pump.fun API для создания пула, а затем формирует Jito-бандл из 20 транзакций покупки. Реализация на Solana Web3.js – достаточно «минималистичный» скрипт.

Архитектура: Node.js/TypeScript. В main.ts создаётся пул и сразу формируется Jito-бандл из 20 транзакций (через bundle()), гарантируя атомарность.

Производительность: Одновременная покупка 20 кошельками за один слот
github.com
 (рядом друг с другом), что в теории даёт около 20× выше скорость, чем по одному.

Применение: Запуск и первичные продажи новых токенов на Pump.fun. Подходит тем, кто хочет именно скорость и надёжность Jito-бандла без дополнительных наворотов.

Отзывы: Широко применяется среди сообщества Pump.fun. Репозиторий содержит MIT-лицензию и регулярно обновляется (последний апдейт октябрь 2025). Пользователи отмечают простоту настройки.

m4rcu5o – Solana Bonkfun Bundler

Репозиторий: m4rcu5o/Solana-bonkfun-bundler (TypeScript, 44★). Открытый бандлер для Bonk.fun (LetsBonkFun) с интеграцией Jito. Позволяет создавать токен, автоматически распределять SOL по кошелькам и атомарно совершать мультипокупки, а также включает функции «delay sniping» и очистки кошельков после запуска
github.com
. Технически – это CLI-инструмент на TypeScript с множеством опций.

Архитектура: Node.js/TypeScript, использует Jito для атомарных бандлов и обращается к Bonk SDK для создания токена и пулов. Реализованы несколько режимов: мульти-кошелёчный запуск с бандлом, одиночный «снайп», управление остатками SOL.

Производительность: По умолчанию создаётся пул и несколько кошельков автоматически, затем все кошельки покупают в одном блоке
github.com
. Это обеспечивает максимальную эффективность – все покупки проходят одновременно.

Применение: Автоматический запуск токена на Bonk.fun, с возможностью координированной покупки из множества кошельков. Есть отдельный режим быстрого «снайпа» (скачок цены после запуска). Подходит разработчикам и трейдерам Bonk-коинов.

Отзывы: Репозиторий рекомендуют за гибкость – в нём легко менять число кошельков, слippage и т.д. Как отмечает описание, это «самый гибкий и быстрый бандлер для Bonk.fun»
github.com
.

Niranjanprasad1 – Solana Memecoin Trading Bot (Rust)

Репозиторий: Niranjanprasad1/Solana-Memecoin-Trading-Bot (Rust, 6★). Модульный фреймворк на Rust для мемекойн-стратегий (sniper, bundler, volume, арбитраж). Особенность – гибкая архитектура с ядром на Rust и опциональным TypeScript SDK для UI. Atomic-бандлер здесь – одна из стратегий, исполняемая через Jito
github.com
.

Архитектура: Rust (async Tokio), ядро обеспечивает низкую задержку (модуль «Engine»), наблюдение мемпула и адаптеры под разные протоколы (Raydium, Pump.fun, Bonk.fun, Meteora). Экзекьютор Jito обрабатывает бандлы атомарно
github.com
. В сборке предусмотрены метрики (Prometheus) и командная строка для управления стратегиями.

Производительность: Благодаря Rust Core и Jito-интеграции бот обеспечивает очень низкую латентность. В документации подчёркивается «низкая задержка маршрутов и приоритетный вычислительный ресурс через Jito»
github.com
. Сам бандлер формирует атомарные наборы транзакций (снижение общей задержки и сбор слитых ордеров в один вызов).

Применение: Универсальный набор инструментов: кроме бандлера есть мгновенные снайперы и объёмные боты. Поддерживаются Pump.fun, Bonk.fun, Raydium. Подходит для продвинутых пользователей, готовых интегрировать Rust-библиотеки в свои системы.

Отзывы: Проект представляет собой более «корпоративное» решение, чем простые скрипты, но пока мало распространён (несколько звёзд на GitHub). Тем не менее он демонстрирует, что высокопроизводительные бандлеры можно сделать на Rust с поддержкой Jito.

enlomy – PumpFun Bundler (Vanity, LUT)

Репозиторий: enlomy/pumpfun-bundler (TypeScript, 24★). «Софистицированный» бандлер на Pump.fun с расширенным функционалом. В особенности умеет генерировать удобные vanity-адреса токена (с суффиксом «pump»), создавать Address Lookup Table для оптимизации размера транзакций, автоматически повторять запросы при ошибках, и т.д
github.com
github.com
. Работает в мульти- и одно-кошельковом режимах.

Архитектура: Node.js/TypeScript. Из особенностей – встроенная утилита генерации vanity-адресов, поддержка кастомного LUT-программы. Создаёт пул на Pump.fun, распределяет SOL по нескольким кошелькам, а затем выполняет покупки через Jito-бандлы. Есть вспомогательные скрипты (закрытие LUT, сбор средств).

Производительность: Поддерживает до N кошельков (по умолчанию 10-12), запускает их в координированном бандле. Благодаря Jito количество запросов к сети сокращено, а LUT снижает нагрузку на сеть
github.com
github.com
.

Применение: Быстрый запуск с акцентом на «чистую» сетевую репутацию (vanity/пузырь). Полезен тем, кто хочет тщательную настройку (настройка «SELL» и стоп-лосс по каждому кошельку).

Отзывы: Пользователи хвалят удобство конфигурации (все параметры в .env) и наличие автоматических retry. Этот бот хорошо описывает себя как «один из самых продвинутых бандлеров Pump.fun» благодаря своим «расширенным профилям и защите от MEV»
github.com
.

solana-scripts (cicere) – Pump.Fun Bundler

Репозиторий: cicere/pumpfun-bundler (TypeScript, 470★). Один из самых популярных open-source бандлеров для Pump.fun. Нацелено на максимальную эффективность и безопасность: в нём реализованы анти-пузырьковые механики и подробный CLI-интерфейс. Заявлено: «Ultimate open-source solution for efficient token bundling on Pump.fun with advanced profile creation and anti-bubble features»
github.com
.

Архитектура: Node.js/TypeScript, запускается через командную строку. Пользователю предлагается создать конфиг-профили (переименовывать названия кошельков, параметры слippage и пр.). Мульти-кошелечный режим обеспечивает мгновенное распределение и покупку до 25 кошельков одновременно
github.com
. Поддерживаются Lookup Tables для оптимизации, есть встроенные «SafeMath»-проверки, автоматическое закрытие ATAs и возврат SOL.

Производительность: По умолчанию выполняет создание пула и 25 координированных покупок одним Jito-бандлом
github.com
. За счёт этого и оптимизации LUT достигается высочайшая пропускная способность – реже попадает в мемпул и почти всегда сразу включается в блок. Пользовательский интерфейс (CLI) при этом работает быстро и надёжно.

Применение: Широко используется комьюнити Pump.fun. Предназначен для тех, кто хочет «готовое к бою» решение со множеством настроек профилей, чтобы избежать флагов «пузырь-карт». Проект активно поддерживается (470 звёзд).

Отзывы: Сообщество считает этот бандлер «лучшим» по функциям. В описании подчёркивается, что он остаётся «лучшим с 2024» года
github.com
, а сами пользователи отмечают его анти-спам решения и подробную документацию.

Rezzecup – Pump.Fun Bundler (15 wallets)

Репозиторий: Rezzecup/pumpfun-sol-bundler (TypeScript, 36★). Мультиязыковой бандлер с собственным UI, позиционирующий себя как «free, open-source, efficient self-bundling script». Поддерживает запуск со 15 кошельками и генерирует «поддельные» профили для обхода пузырей
github.com
.

Архитектура: Node.js/TypeScript + веб-интерфейс (React). Утверждает, что «упрощает процесс» через UI, но в основе – скрипт, который конфигурируется через .env. В ходе старта создаёт пул, распределяет кошельки (до 15) и запускает их совместную покупку. Есть встроенная LUT-оптимизация и дополнительные «супер-функции» (управление раздачей токена и сложные sell-стратегии).

Производительность: Осуществляет 15 одновременных покупок в одном блоке
github.com
. В описании отмечается «быстрое, стабильное и эффективное» выполнение и упор на надёжность.

Применение: Подойдёт тем, кто хочет UI+CLI решение. Бандлер позиционируется как удобный для «одного человека» (есть пошаговый интерфейс) и работает на Pump.fun с учётом защит от Photon-пультов (bot-атаки).

Отзывы: Упоминают, что проект хорошо описан и прост в применении. Однако по сравнению с другими он менее оптимизирован (15 кошельков – меньше, чем 20–25 у конкурентов). Пользователи отмечают, что нацелено на удобство, а не на экстремальную скорость.

Комментарии сообщества

Многие трейдеры отмечают, что самописный Jito-бандлер даёт лучшие результаты, чем «черные ящики» типа Infinity или DogWif (за 10–15 SOL)
reddit.com
. Например, один опытный участник (Vtrader_io) советует освоить принципы работы Jito и самостоятельно собрать бандл, поскольку никакой сторонний инструмент не даст «пропорциональных преимуществ»
reddit.com
.

В то же время в дискуссиях упоминают и бесплатные альтернативы: один из участников указывал на свой проект tokariapp (оформленный как бесплатное ПО), заявляя, что он «лучше дорогих бандлеров»
reddit.com
. Это свидетельствует о том, что высокопроизводительные решения появляются открытым исходным кодом.

Эти проекты – лишь часть экосистемы. Все они акцентируют внимание на атомарном мульти-кошельковом исполнении через Jito, что и даёт прирост в 5–10+ раз по скорости по сравнению с традиционными бандлерами. Для построения собственного бандлера лучше брать за основу проверенные open-source библиотеки выше – они уже реализуют самые современные оптимизации и прошли испытание на реальных запусках