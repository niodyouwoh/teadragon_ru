# 🗄️ Настройка Vercel Postgres

## Проблема
SQLite не работает на Vercel из-за serverless архитектуры. Нужна облачная база данных.

## Решение: Vercel Postgres

### Шаг 1: Создайте Postgres базу в Vercel

1. Откройте ваш проект на Vercel
2. Перейдите во вкладку **Storage**
3. Нажмите **Create Database**
4. Выберите **Postgres**
5. Выберите регион (ближайший к вашим пользователям, например **Frankfurt** для России)
6. Нажмите **Create**

### Шаг 2: Подключите базу к проекту

1. После создания базы, Vercel автоматически добавит переменные окружения:
   - `POSTGRES_URL`
   - `POSTGRES_PRISMA_URL`
   - `POSTGRES_URL_NON_POOLING`
   - `POSTGRES_USER`
   - `POSTGRES_HOST`
   - `POSTGRES_PASSWORD`
   - `POSTGRES_DATABASE`

2. Эти переменные автоматически доступны в вашем проекте

### Шаг 3: Инициализируйте базу данных

После деплоя откройте в браузере:

```
https://ваш-сайт.vercel.app/api/init-db
```

Вы должны увидеть:
```json
{
  "success": true,
  "message": "Database initialized successfully"
}
```

Это создаст все таблицы и администратора.

### Шаг 4: Войдите в админку

1. Откройте `/admin`
2. Логин: `shebo`
3. Пароль: `shebo12345`

## Что было сделано

✅ Установлен `@vercel/postgres`
✅ Создан `lib/db-postgres.ts` с функциями для работы с Postgres
✅ Создан API endpoint `/api/init-db` для инициализации базы
✅ Все функции адаптированы под Postgres

## Следующие шаги

1. **Отправьте код на GitHub:**
```bash
git add .
git commit -m "Add Vercel Postgres support"
git push origin main
```

2. **Создайте Postgres базу в Vercel** (см. Шаг 1 выше)

3. **Дождитесь деплоя** (Vercel автоматически задеплоит после push)

4. **Инициализируйте базу:** откройте `/api/init-db`

5. **Войдите в админку** и добавьте товары

## Преимущества Vercel Postgres

✅ Данные сохраняются между деплоями
✅ Автоматическое масштабирование
✅ Бесплатный план: 256 MB хранилища, 60 часов compute time
✅ Автоматические бэкапы
✅ Connection pooling

## Бесплатный план

- **Storage**: 256 MB
- **Compute**: 60 часов в месяц
- **Rows**: ~500,000 строк

Этого достаточно для небольшого магазина!

## Альтернативы (если нужно больше)

1. **Supabase** - бесплатно до 500 MB
2. **Neon** - бесплатно до 3 GB
3. **PlanetScale** - бесплатно до 5 GB

## Миграция данных (если нужно)

Если у вас уже есть данные в SQLite локально, можно экспортировать и импортировать:

1. Экспортируйте данные из SQLite
2. Используйте Vercel Postgres Dashboard для импорта
3. Или создайте API endpoint для миграции

## Проверка подключения

После создания базы проверьте переменные окружения:

**Settings** → **Environment Variables**

Должны быть:
- ✅ `POSTGRES_URL`
- ✅ `JWT_SECRET`
- ✅ `S3_ENDPOINT`
- ✅ `S3_REGION`
- ✅ `S3_BUCKET`
- ✅ `S3_ACCESS_KEY`
- ✅ `S3_SECRET_KEY`

## Готово!

После выполнения всех шагов ваш магазин будет полностью работать на Vercel с облачной базой данных! 🎉
